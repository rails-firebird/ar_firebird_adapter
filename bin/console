#!/usr/bin/env ruby

require "bundler/setup"
require_relative "../lib/active_record/connection_adapters/ar_firebird_adapter"

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start

require "irb"
IRB.start(__FILE__)
